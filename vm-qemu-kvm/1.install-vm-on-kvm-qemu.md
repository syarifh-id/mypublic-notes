# Manajemen VM dengan libvirt, virsh, VNC, dan SPICE

## 0. Install QEMU/KVM

```bash
sudo apt update
sudo apt install qemu-kvm libvirt-daemon-system libvirt-clients bridge-utils virt-manager cloud-utils
```
## 1. Dasar Penggunaan virsh

-   Melihat daftar VM: `virsh list --all`
-   Start / stop VM: `virsh start`, `virsh shutdown`, `virsh destroy`
-   Edit konfigurasi VM: `virsh edit nama_vm`
-   Melihat XML VM: `virsh dumpxml nama_vm`
-   Console CLI: `virsh console nama_vm`

------------------------------------------------------------------------

## 2. Storage Pool

-   Melihat pool: `virsh pool-list --all`
-   Stop pool: `virsh pool-destroy nama_pool`
-   Hapus definisi: `virsh pool-undefine nama_pool`
-   Refresh pool: `virsh pool-refresh nama_pool`
-   Lokasi pool bisa di luar `/var/lib/libvirt/`
-   Konfigurasi pool disimpan di:
    -   `/etc/libvirt/storage/`

------------------------------------------------------------------------

## 3. Lokasi Konfigurasi Penting

-   Konfigurasi VM: `/etc/libvirt/qemu/`
-   Snapshot metadata: `/etc/libvirt/qemu/snapshot/`
-   Log VM: `/var/log/libvirt/qemu/`
-   Disk VM: sesuai lokasi storage pool

------------------------------------------------------------------------
### install from ISO 

```bash
virt-install \
  --name nama-vm \
  --memory 4096 \
  --vcpus 2 \
  --cpu host-passthrough \
  --disk path=~/disk/ke.qcow2,size=16,bus=virtio,cache=none,io=native \
  --cdrom ~/disk/ke.iso \
  --network network=default,model=virtio \
  --graphics spice,listen=none \
  --video virtio \
  --channel spicevmc \
  --osinfo debian12 \
  --rng /dev/urandom
```

### Import qcow2

```bash
virt-install \
  --name nama-vm \
  --memory 4096 \
  --vcpus 2 \
  --cpu host-passthrough \
  --disk path=~/disk/ke.qcow2,bus=virtio,cache=none,io=native \
  --network network=default,model=virtio \
  --graphics spice,listen=none \
  --video virtio \
  --channel spicevmc \
  --osinfo debian12 \
  --rng /dev/urandom \
  --import
```
--cloud-init user-data=config.yaml

config.yml

```yml
#cloud-config
user: rocky
password: 1234
chpasswd: { expire: False }
ssh_pwauth: True
```

console
``` bash
virsh console nama_vm
```

## VNC

```bash
virt-install \
  --name nama-vm \
  --memory 4096 \
  --vcpus 2 \
  --cpu host-passthrough \
  --disk path=~/disk/ke.qcow2,size=16,bus=virtio,cache=none,io=native \
  --cdrom ~/disk/ke.iso \
  --network network=default,model=virtio \
  --graphics vnc,listen=0.0.0.0 \
  --video virtio \
  --osinfo debian12 \
  --rng /dev/urandom
  
```


```bash
virt-install \
  --name nama-vm \
  --memory 4096 \
  --vcpus 2 \
  --cpu host-passthrough \
  --disk path=~/disk/ke.qcow2,bus=virtio,cache=none,io=native \
  --network network=default,model=virtio \
  --graphics vnc,listen=0.0.0.0 \
  --video virtio \
  --osinfo debian12 \
  --rng /dev/urandom \
  --import
```

virsh vncdisplay nama-vm
